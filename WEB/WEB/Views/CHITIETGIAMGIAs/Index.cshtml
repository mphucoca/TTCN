@model IEnumerable<WEB.Models.CHITIETGIAMGIA>

@{
    ViewBag.Title = "Danh sách chi tiết giảm giá";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
          Mô tả
        </th>
        <th>
           Tên sản phẩm
        </th>
        <th>
           Mức giảm giá
        </th>
        <th>
           Giá mua để áp dụng
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GIAMGIA.MoTa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SANPHAM.TenSanPham)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MucGiamGia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Giamua)
        </td>
        <td>
            @Html.ActionLink("Chỉnh sửa", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Chi tiết", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Xóa", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
